# Claude Code Instructions


## Project Spec

Always read [docs/ProjectSpec.md](docs/ProjectSpec.md) at the start of each conversation for full project context and requirements.

## Environment

This project runs inside a **dotnetbox distrobox** container. Always prefix shell/npm commands with `distrobox enter dotnetbox --`:

TBD: Add documentation for build, test and run commands here
